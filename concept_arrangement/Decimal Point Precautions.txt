<자바스크립트에서 소수 연산 시 주의점>

문제점
소수점 연산 시 딱 떨어지지 않고 결과값이 약간 다름
ex. 1.1 + 0.3 = 1.40000000001

해결 방법?
1. 덧셈하기 전에 10 곱해서 덧셈하고 10으로 나누기
2. 외부라이브러리 사용
3. 반올림하기
    -> 숫자.toFixed(몇자리) 사용 [문자로 변환되므로 숫자로 변환해야 함]
    -> parseInt()과 parseFloat()을 이용해 숫자로 변환
    -> parseInt()는 정수로, parseFloat()는 실수로 변환함.